# Plan: Duxio - Automated Coach Acquisition System

- **Goal**: Implement the Duxio Coach Acquisition Engine, enabling coaches to run automated funnel audits and follow-up sequences using the existing `duxio-coaches` stack.
- **Current Context**: Existing Tanstack Start/Vite frontend (`D:/DUXIO NEW/duxio-coaches`). Repositories and AI engines (`custom_tools/`) available.
- **Architecture**: Extend the existing `audits.$token.tsx` route to trigger an autonomous "Free Coach Funnel Audit" using the `AutoRAGOrchestrator` to analyze website leaks vs. best-practice funnels.

---

## Step-by-Step Tasks

### 1. Plan Initialization

- [ ] Create plan file at `D:/DUXIO NEW/duxio-coaches/.hermes/plans/2026-09-19_210000-duxio-launch.md`.

### 2. Implementation - Audit Logic

- [ ] Task 2.1: Implement audit logic in `D:/DUXIO NEW/duxio-coaches/src/lib/audit.functions.ts` to score landing pages against the 4-part Duxio framework (Attract, Capture, Nurture, Book).
- [ ] Task 2.2: Create a mock response generator for the audit in `src/lib/audit-schema.server.ts` to bypass live Supabase/Stripe for now.
- [ ] Task 2.3: Modify `src/routes/audits.$token.tsx` to display the "Coach Funnel Audit" results, showing "Leaks" (e.g., manual follow-up required) and "Remedies" (Duxio system automation).

### 3. Implementation - UI/UX

- [ ] Task 3.1: Update `src/routes/index.tsx` to prominently feature the "Free Coach Funnel Audit" hero section (using existing `components/ui/` Radix library).
- [ ] Task 3.2: Implement the "Audit Launch" button and link it to the auditor route.

### 4. Validation / TDD

- [ ] Task 4.1: Run `npm run lint` in `D:/DUXIO NEW/duxio-coaches/` to ensure no errors added.
- [ ] Task 4.2: Run `npm run build` to verify the new audit frontend compiles.

---

## Risks, Tradeoffs, and Open Questions

- **Risk**: Automated system dependency on Supabase/Stripe.
- **Tradeoff**: Using fake/mock audit data in `server.ts` prevents prod database breaks but might lead to hardcoded "ghost" data if not explicitly marked.
- **Open Question**: Does the user want the backend to actually _perform_ the audit live via subagents, or serve cached templates? (Plan assumes live subagent logic for now).
